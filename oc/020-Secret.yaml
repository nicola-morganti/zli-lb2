# Database
kind: Secret
apiVersion: v1
metadata:
  name: mysqlpassword # Change only if you also update Deployment secretKeyRef.name.
  labels:
    app: quotes-app
    app.kubernetes.io/part-of: quotes-app
data:
  password: YWRtaW4= # Base64 value; rotate for security and never keep real secrets in git.
type: Opaque # Keep Opaque for generic key/value credentials.
